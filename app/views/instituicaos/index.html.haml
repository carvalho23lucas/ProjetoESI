.separador1
.h1 Lista de Instituições

.separador1
.separador1
.separador
- @instituicaos.each do |instituicao|
  .itemObjeto
    .itemObjetoIcone
      -@imagem = "/images/logoinst.png"
      %img{:alt => "Doador Pessoa", :src => @imagem, :width => "90px"}/
    .itemObjetoNome
      %h1
        = instituicao.nome
      .h2 
        = instituicao.area_atuacao.nome
    %a{:href => "instituicaos/" + instituicao.id.to_s}    
      .itemObjetoBotao
        .buttonlink
          VER
    .itemObjetoMeta
      %h2   
        = instituicao.email
      .h3
        = instituicao.cidade.nome
        = "-"
        = Estado.where(id: instituicao.cidade.estado.id).first.sigla
  .separador4
.separador1
.separador1
%a{:href => "/home/home"} 
  .button1
    .buttonlink VOLTAR
.separador1
.separador1
.separador2
